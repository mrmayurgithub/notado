enum listSortType { name, date }

enum zefyrNoteMode { newNote, editNote }

enum currentScreen {
  home,
  trash,
  settings,
  study,
  login,
  search,
}

enum notesView { list, grid }

enum currentTheme { light, dark }
